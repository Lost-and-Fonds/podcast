# Stashd Podcast plugin

Planned Podcast Broadcast package for Stashd. The production-native provider is
not implemented yet; M10 owns that work. The former Wasm implementation remains
historical reference material in `Lost-and-Fonds/stashd`.

When M10 starts, this repository will add the native package manifest,
entrypoint, provider implementation, fixtures, and SDK compatibility tests.
